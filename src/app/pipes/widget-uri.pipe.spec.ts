import { WidgetUriPipe } from './widget-uri.pipe';

describe('WidgetUriPipe', () => {
  it('create an instance', () => {
    const pipe = new WidgetUriPipe();
    expect(pipe).toBeTruthy();
  });
});
